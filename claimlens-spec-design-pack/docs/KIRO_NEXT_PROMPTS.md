
# Kiro — Next-Step Prompts (UI + Tasks)

## 1) Admin Console Scaffold
Create a minimal Admin Console under /app/web (Vite + TS + React).
Use /app/web/design-tokens.css and components.css.
Pages: /dashboard, /profiles, /profiles/:id, /packs, /fixtures, /audits/:id.
Dark theme, header/nav, KPI cards, tables, Augment‑Lite modal, Degraded Mode banner.
Mock data only; follow docs/UX_SPEC.md.

## 2) Overlay (ClaimLens Go) Prototype
Create /app/ext (Chrome MV3) with a content script that overlays badges on fixtures/sites/sample.html, plus a side panel with “why” tooltips and a locale toggle.

## 3) Task Plan Refresh
Regenerate docs/TASKS.md with Week 1–4 milestones covering UI + overlay, with DoD checklists.

## 4) CI Notes
Update README of main repo with sections for Admin Console, Overlay Dev Mode, and Kiro receipts (/ .kiro).
